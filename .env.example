# Google Sheets — джерело даних про товари (src/services/googleSheets.ts)
# Наразі не підключено до сторінок (таблиця містить лише тестовий рядок) —
# заповніть реальні дані в таблиці і підключіть getProducts() у page.tsx, коли будете готові.
GOOGLE_API_KEY=
GOOGLE_SPREADSHEET_ID=

# Базовий URL сайту для метаданих/OpenGraph/JSON-LD (без завершального слеша)
NEXT_PUBLIC_SITE_URL=https://furnit-pro.vercel.app
